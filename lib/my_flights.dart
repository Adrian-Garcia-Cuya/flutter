import 'package:flutter/material.dart';

class MyFlights extends StatelessWidget {
  const MyFlights({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Mis vuelos');
  }
}
